# Build output

Place the compiled add-in here:

`ExcelVbaLib.xlam`

Create it once (Excel → Save As → Excel Add-in), import modules from `../source/`, then keep using this file as the runtime library.

Binary `.xlam` files are gitignored — commit source under `../source/` instead.
