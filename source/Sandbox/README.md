# Sandbox

Templates, experiments, and incomplete code. **Do not import into `ExcelVbaLib.xlam` until deliberately promoted.**

Personal: `z_*`, `zStockCode*`, `z_WIP`, `zCollectionTools`.

Harvest useful snippets into `Internal/` or `Features/`; leave the originals here or delete once copied.
