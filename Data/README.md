# Data

| File | Role |
|------|------|
| `Personal123.xlsb` | Personal macro workbook (source of VBA export); local only, gitignored |

Keep binaries out of git. The runtime library is `build/ExcelVbaLib.xlam` (gitignored). `source/` is only a snapshot of modules already in the add-in.
